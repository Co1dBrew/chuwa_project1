/// <reference types="vite/client" />

// This one-line reference pulls in the type declarations that Vite provides.
// Among other things, it teaches TypeScript that importing a ".css" file for its
// side effects (as we do in main.tsx) is allowed.
